# ══════════════════════════════════════════════════════════
#  PC Agent — Python Dependencies
#  Install: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════

# ── Core ────────────────────────────────────────────────
discord.py
python-dotenv

# ── System monitoring ───────────────────────────────────
psutil

# ── HTTP / API ──────────────────────────────────────────
httpx

# ── Database ────────────────────────────────────────────
# (uses built-in sqlite3)

# ── Visualizations ─────────────────────────────────────
matplotlib
numpy

# ── Screenshots ─────────────────────────────────────────
mss
Pillow

# ── Keyboard / Mouse automation ─────────────────────────
pyautogui

# ── Clipboard ───────────────────────────────────────────
pyperclip

# ── Text-to-Speech ──────────────────────────────────────
pyttsx3

# ── Voice recognition ───────────────────────────────────
SpeechRecognition
# pyaudio                 # Uncomment; may need system package first:
#                         # Ubuntu: apt install portaudio19-dev python3-pyaudio
#                         # macOS:  brew install portaudio
#                         # Windows: pip install pipwin && pipwin install pyaudio

# ── Audio recording ─────────────────────────────────────
sounddevice
soundfile

# ── Windows-specific (comment out on Linux/macOS) ───────
pycaw                  # Windows audio control
wmi                    # Windows Management Instrumentation
comtypes               # COM interface (pycaw dependency)

# ── GPU monitoring (optional) ───────────────────────────
GPUtil                 # NVIDIA GPU stats

# ── Network speed test (optional) ───────────────────────
speedtest-cli

nvidia-ml-py3          # Deep NVIDIA GPU telemetry via NVML (gpu_detailed cog)

# ── Native C++ binaries (compile separately or use !build commands) ────────
# native/perf_counters.exe  — cl /EHsc /O2 perf_counters.cpp /link pdh.lib
# native/fps_counter.exe    — cl /EHsc /O2 fps_counter.cpp /link dxgi.lib user32.lib
# native/fps_hook.dll       — cl /EHsc /O2 /LD fps_hook.cpp /link dxgi.lib d3d11.lib
# native/gpu_pipeline.exe   — cl /EHsc /O2 gpu_pipeline.cpp /link pdh.lib dxgi.lib
# MinGW alternative: g++ -std=c++17 -O2 <src.cpp> -l<lib> -o <out>
